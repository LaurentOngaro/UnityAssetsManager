openapi: 3.0.3
info:
  title: UnityAssetsManager API
  version: 1.2.18
  description: API locale pour explorer et exporter des assets Unity (CSV/SQLite). Le port HTTP par défaut est 5003 et peut être modifié dans config/config.json.
servers:
  - url: http://localhost:5003
paths:
  /api/data:
    get:
      summary: Liste paginée des données assets
      parameters:
        - in: query
          name: draw
          schema:
            type: integer
        - in: query
          name: start
          schema:
            type: integer
        - in: query
          name: length
          schema:
            type: integer
        - in: query
          name: search[value]
          schema:
            type: string
        - in: query
          name: search[regex]
          schema:
            type: boolean
      responses:
        '200':
          description: Données paginées
          content:
            application/json:
              schema:
                type: object
                properties:
                  draw:
                    type: integer
                  recordsTotal:
                    type: integer
                  recordsFiltered:
                    type: integer
                  data:
                    type: array
                    items:
                      type: object
  /api/columns:
    get:
      summary: Liste des colonnes disponibles
      responses:
        '200':
          description: Colonnes
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
  /api/templates:
    get:
      summary: Liste des templates d'export
      responses:
        '200':
          description: Templates
          content:
            application/json:
              schema:
                type: object
                properties:
                  templates:
                    type: array
                    items:
                      type: object
                      properties:
                        name:
                          type: string
                        description:
                          type: string
                        pattern:
                          type: string
  /api/profiles:
    get:
      summary: Liste des profils
      responses:
        '200':
          description: Profils
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
    post:
      summary: Sauvegarder un profil
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProfileSaveRequest'
      responses:
        '200':
          description: Profil sauvegardé
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  message:
                    type: string
        '400':
          description: Requete invalide
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          description: Erreur interne
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
  /api/profiles/{name}:
    get:
      summary: Charger un profil
      parameters:
        - in: path
          name: name
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Profil
          content:
            application/json:
              schema:
                type: object
        '404':
          description: Profil absent
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
    delete:
      summary: Supprimer un profil
      parameters:
        - in: path
          name: name
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Profil supprimé
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  message:
                    type: string
        '404':
          description: Profil absent
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
  /api/export:
    post:
      summary: Exporter les données filtrées
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExportRequest'
      responses:
        '200':
          description: Fichier exporté (binaire)
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400':
          description: Erreur de requête ou de template
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          description: Erreur export
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
  /api/batch-export:
    post:
      summary: Exporter les données en mode headless vers un fichier local
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BatchExportRequest'
      responses:
        '200':
          description: Export batch écrit sur disque
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  file:
                    type: string
                  format:
                    type: string
                  count:
                    type: integer
        '400':
          description: Requête batch invalide
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          description: Erreur de batch export
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
  /api/reload:
    post:
      summary: Recharger les données depuis la source
      responses:
        '200':
          description: Rechargement effectué
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  message:
                    type: string
  /api/stats:
    get:
      summary: Statistiques sur la source courante
      responses:
        '200':
          description: Statistiques
          content:
            application/json:
              schema:
                type: object
        '404':
          description: Aucune donnee chargee
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
  /api/test:
    get:
      summary: Healthcheck backend pour automatisation
      responses:
        '200':
          description: Etat backend
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  timestamp:
                    type: string
                    format: date-time
                  version:
                    type: string
                  data_path:
                    type: string
                    nullable: true
                  source_type:
                    type: string
                    nullable: true
                  has_data:
                    type: boolean
  /api/setup:
    post:
      summary: Configurer la source de données
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetupRequest'
      responses:
        '200':
          description: Configuration appliquée
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  message:
                    type: string
        '400':
          description: Paramètres de configuration invalides
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          description: Erreur de configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
  /api/config:
    get:
      summary: Lire la configuration runtime publique
      responses:
        '200':
          description: Configuration courante
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  config:
                    $ref: '#/components/schemas/PublicRuntimeConfig'
    post:
      summary: Mettre a jour la configuration runtime autorisee
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfigUpdateRequest'
      responses:
        '200':
          description: Configuration mise a jour
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  message:
                    type: string
                  config:
                    $ref: '#/components/schemas/PublicRuntimeConfig'
        '400':
          description: Payload de configuration invalide
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          description: Erreur interne
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
  /api/test-path:
    post:
      summary: Vérifier un chemin source CSV/SQLite
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                path:
                  type: string
      responses:
        '200':
          description: Resultat du test
          content:
            application/json:
              schema:
                type: object
                properties:
                  exists:
                    type: boolean
                  is_dir:
                    type: boolean
                  type:
                    type: string
                  source_type:
                    type: string
                  rows:
                    type: integer
                  cols:
                    type: integer
                  tables:
                    type: array
                    items:
                      type: string
                  warning:
                    type: string
components:
  schemas:
    ApiErrorEnvelope:
      type: object
      properties:
        error:
          $ref: '#/components/schemas/ApiError'
      required:
        - error
    ApiError:
      type: object
      properties:
        code:
          type: string
        message:
          type: string
        http_status:
          type: integer
        timestamp:
          type: string
          format: date-time
        path:
          type: string
        details:
          type: object
          additionalProperties: true
      required:
        - code
        - message
        - http_status
        - timestamp
        - path
        - details
    SetupRequest:
      type: object
      properties:
        data_path:
          type: string
        db_table:
          type: string
      required:
        - data_path
    ProfileSaveRequest:
      type: object
      properties:
        name:
          type: string
        columns:
          type: array
          items:
            type: string
        filter_stack:
          type: array
          items:
            type: object
      required:
        - name
    ExportRequest:
      type: object
      properties:
        template:
          type: string
        columns:
          type: array
          items:
            type: string
        search:
          type: string
        filter_stack:
          type: array
          items:
            type: object
        alias_map:
          type: object
          additionalProperties:
            type: string
    BatchExportRequest:
      allOf:
        - $ref: '#/components/schemas/ExportRequest'
        - type: object
          properties:
            output_dir:
              type: string
            file_name:
              type: string
            output_path:
              type: string
            profile:
              type: string
    PublicRuntimeConfig:
      type: object
      properties:
        data_path:
          type: string
          nullable: true
        db_table:
          type: string
        show_parser_warnings:
          type: boolean
        server_host:
          type: string
        server_port:
          type: integer
        flask_debug:
          type: boolean
        flask_threaded:
          type: boolean
        max_content_length_mb:
          type: integer
        cache_ttl_seconds:
          type: integer
        default_page_size:
          type: integer
        log_level:
          type: string
        log_output:
          type: string
          enum: [console, file, both]
        log_max_bytes:
          type: integer
        log_backup_count:
          type: integer
    ConfigUpdateRequest:
      type: object
      properties:
        db_table:
          type: string
        show_parser_warnings:
          type: boolean
        log_level:
          type: string
          enum: [DEBUG, INFO, WARNING, ERROR, CRITICAL]
        log_output:
          type: string
          enum: [console, file, both]
        log_max_bytes:
          type: integer
          minimum: 1
        log_backup_count:
          type: integer
          minimum: 1
